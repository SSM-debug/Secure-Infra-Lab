[defaults]
# Path to inventory file — where Ansible finds all servers
inventory = /home/vagrant/ansible/inventory.ini

# Skip SSH host key verification — safe in local lab, not in production
host_key_checking = False

# Allow tmp files to be readable — required for some Ansible modules
allow_world_readable_tmpfiles = True