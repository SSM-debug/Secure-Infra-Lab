---
# Install Cockpit — web-based dashboard for system monitoring
# Access via https://localhost:9090
- name: Install Cockpit
  apt:
    name: cockpit
    state: present
    update_cache: yes

# Start and enable Cockpit service
- name: Enable and start Cockpit
  service:
    name: cockpit
    state: started
    enabled: yes